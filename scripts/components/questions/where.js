// grab when and change the text

const answerWhere = document.querySelectorAll('.main-question-Where .main-question-answer p')[0];

let whereText = "Remote and travel for contracts. Relocation for long term positions.";

answerWhere.textContent = whereText;